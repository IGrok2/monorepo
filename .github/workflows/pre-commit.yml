name: Pre-commit

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  pre-commit:
    runs-on: ubuntu-latest 
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Run pre-commit
        run: |
          eval "$(direnv export bash)"
          direnv reload
          pre-commit run --all-files
        shell: bash
